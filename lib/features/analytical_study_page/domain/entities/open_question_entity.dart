class OpenQuestionEntity {
  final int id;
  final String questionText;
  final String answer;

  const OpenQuestionEntity({
    required this.id,
    required this.questionText,
    required this.answer,
  });
}